@if (User.Identity != null && User.Identity.IsAuthenticated)
    {
        <ul class="nav navbar-nav">
                  <li class="dropdown">
                        <a href="@Url.Action("Index", "Post")" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Посты <span class="caret"></span></a>
                        <ul class="dropdown-menu" role="menu">
                            <li>@Ajax.ActionLink("Мои посты","MyPosts", "Post", new { }, new AjaxOptions{ UpdateTargetId = "content" })</li>
                            <li>@Ajax.ActionLink("Избранное","Favorite", "Post", new { }, new AjaxOptions{ UpdateTargetId = "content" })</li>
                            <li>@Ajax.ActionLink("Лента","News", "Post", new { }, new AjaxOptions{ UpdateTargetId = "content" })</li>
                        </ul>
                  </li>
                  <li class="dropdown">
                        <a href="@Url.Action("Index", "Account")" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Аккаунт <span class="caret"></span></a>
                        <ul class="dropdown-menu" role="menu">
                            <li><a href="@Url.Action("AboutMe", "Account")" id="about_me">О себе</a></li>
                            <li><a href="@Url.Action("DeleteAc", "Account")" id="delete_me">Удалить аккаунт</a></li>
                        </ul>
                  </li>
                  <li><a href="@Url.Action("Help", "Home")" id="help">Помощь</a></li>
                  <li><a href="@Url.Action("Index", "Search")" id="searchpost">Поиск</a></li> 
            @if (User.IsInRole("Moder"))
                {
                   @Html.Partial("~/Views/Admin/ModerMenu.cshtml")
                }
             @if (User.IsInRole("Admin"))
             {      
                   @Html.Partial("~/Views/Admin/AdminMenu.cshtml")
             }
              </ul>
}
else
{
    <ul class="nav navbar-nav">
        <li>@Ajax.ActionLink("Поиск","Index", "Search", new { }, new AjaxOptions{ UpdateTargetId = "content" })</li>
    </ul> 
}